# Repository instructions

- Do not push commits, tags, releases or code to GitHub until Ryan explicitly approves.
- Use intent-based branches such as `feature/` or `fix/`.
- Test all file-management operations against disposable installations.
- Never run map installation tests against the real mapping editor folder.
